namespace Projekt_databas_och_w_system.Models
{
    // skapa en en egen värdetyp som tar in fördefinierade värden
    public enum BoxResult
    {
        // fördefinierade värdena till en box som innehåll
        Empty, Bomb, Gold
    }
}
